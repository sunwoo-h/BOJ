#include <bits/stdc++.h>
using namespace std;


class Queue{
  private:
    static const int MAX = 100;
    arr[MAX];
    int front;
    int rear;
    int size;
  
  public:
    Queue() : front(0), rear(0), size(0) {}
    
    bool enqueue(int vaule){
      if(size == MAX) return false;
      
    }

};

int main() {

  return 0;
}