#include <bits/stdc++.h>
using namespace std;

int main() {
  int A, B;

  while (cin >> A >> B) {  // 입력이 잘 들어오면 true
    // 이렇게 while문 조건부에 수식을 넣을 수도 있음!
      cout << A + B << '\n';
  }

  return 0;
}